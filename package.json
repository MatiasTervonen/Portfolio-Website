{
  "name": "portfolio-website",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./input.css -o ./css/main.css",
    "watch": "npx @tailwindcss/cli -i ./input.css -o ./css/main.css --watch",
    "dev": "concurrently \"pnpm watch\" \"live-server --port=3000 --watch=index.html,css,js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "tailwindcss": "^4.1.12"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "live-server": "^1.2.2"
  }
}
